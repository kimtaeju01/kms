/// <reference types="node" />
import { KeyringNode, NodeMaterialsManager } from '@aws-crypto/material-management-node';
import { EncryptStreamInput } from './encrypt_stream';
import { Readable } from 'stream';
import { MessageHeader } from '@aws-crypto/serialize';
interface EncryptInput extends EncryptStreamInput {
    encoding?: BufferEncoding;
}
export interface EncryptOutput {
    result: Buffer;
    messageHeader: MessageHeader;
}
export declare function encrypt(cmm: KeyringNode | NodeMaterialsManager, plaintext: Buffer | Uint8Array | Readable | string | NodeJS.ReadableStream, op?: EncryptInput): Promise<EncryptOutput>;
export {};
//# sourceMappingURL=encrypt.d.ts.map